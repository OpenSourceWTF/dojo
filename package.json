{
  "name": "@opensourcewtf/dojo",
  "version": "0.1.0",
  "private": true,
  "description": "Dojo - A package manager for AI agent skills",
  "author": "OpenSourceWTF",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/opensourcewtf/dojo.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}